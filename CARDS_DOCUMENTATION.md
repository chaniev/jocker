# Классы для отрисовки карт - Документация

## Обзор

Реализован полный набор классов для отрисовки и управления игральными картами в SpriteKit для игры Jocker.

## Созданные классы

### 1. **Card.swift** - Модель карты
Основная модель данных для карты.

**Компоненты:**
- `Suit` - Масти (бубны ♦️, черви ♥️, пики ♠️, крести ♣️)
- `Rank` - Ранги (6-10, валет, дама, король, туз)
- `Card` - Структура карты с поддержкой джокеров

**Основные методы:**
```swift
// Создание обычной карты
let card = Card(suit: .diamonds, rank: .ace)

// Создание джокера
let joker = Card(joker: true)

// Сравнение карт (с учётом козыря)
if card1.beats(card2, trump: .hearts) {
    print("Первая карта бьёт вторую")
}
```

### 2. **Deck.swift** - Колода карт
Управление колодой из 36 карт с 2 джокерами.

**Основные методы:**
```swift
let deck = Deck()
deck.shuffle()  // Перемешать колоду

// Взять карту
if let card = deck.drawCard() {
    print("Взята карта: \(card.description)")
}

// Раздать карты игрокам
let result = deck.dealCards(playerCount: 4, cardsPerPlayer: 3)
let hands = result.hands        // Руки игроков
let trump = result.trump        // Козырная карта
```

### 3. **CardNode.swift** - Визуальное представление карты
SpriteKit нода для отображения карты на экране.

**Основные возможности:**
- Отрисовка лицевой стороны карты с мастью и рангом
- Отрисовка рубашки карты с узором
- Специальное оформление для джокера
- Анимация переворота карты
- Подсветка карты
- Анимации перемещения

**Использование:**
```swift
let cardNode = CardNode(card: card, faceUp: true)
cardNode.position = CGPoint(x: 100, y: 100)
scene.addChild(cardNode)

// Перевернуть карту
cardNode.flip(animated: true)

// Подсветить карту
cardNode.highlight(true, color: .yellow)

// Анимация перемещения
cardNode.animateTakeToHand(to: targetPosition)
```

**Размеры карты:**
- Ширина: 80 точек
- Высота: 120 точек
- Скруглённые углы: 8 точек

### 4. **CardHandNode.swift** - Рука игрока с картами
Управляет коллекцией карт в руке игрока с автоматической раскладкой.

**Основные возможности:**
- Веерная раскладка карт
- Автоматическое расположение карт в руке
- Сортировка карт
- Подсветка карт, которые можно сыграть
- Переворот всех карт
- Анимации добавления/удаления карт

**Использование:**
```swift
let hand = CardHandNode()
hand.handPosition = CGPoint(x: 400, y: 200)
hand.cardSpacing = 70          // Расстояние между картами
hand.arcAngle = 0.4            // Угол веера
hand.isFaceUp = true           // Показывать лицом

// Добавить карты
hand.addCards([card1, card2, card3], animated: true)

// Отсортировать карты
hand.sortCardsStandard(animated: true)

// Подсветить карты, которые можно сыграть
hand.highlightPlayableCards([card1, card3])

// Удалить карту
let removedNode = hand.removeCard(card1, animated: true)
```

### 5. **TrickNode.swift** - Узел для взятки
Управляет картами, выложенными на стол в текущей взятке.

**Основные возможности:**
- Размещение карт на столе
- Определение победителя взятки
- Проверка валидности хода
- Анимация сбора карт

**Использование:**
```swift
let trickNode = TrickNode()
trickNode.centerPosition = CGPoint(x: 400, y: 300)
trickNode.cardRadius = 100  // Радиус размещения карт

// Выложить карту на стол
let cardNode = trickNode.playCard(card, fromPlayer: 1, animated: true)

// Определить победителя
if let winnerPlayer = trickNode.determineWinner(trump: .hearts) {
    print("Победитель: игрок \(winnerPlayer)")
}

// Проверить, можно ли сыграть карту
let canPlay = trickNode.canPlayCard(card, fromHand: playerHand, trump: .hearts)

// Убрать карты со стола
trickNode.clearTrick(toPosition: winnerPosition, animated: true) {
    print("Взятка собрана")
}
```

### 6. **TrumpIndicator.swift** - Индикатор козыря
Визуальный индикатор козырной карты.

**Использование:**
```swift
let trumpIndicator = TrumpIndicator()
trumpIndicator.position = CGPoint(x: 900, y: 400)

// Установить козырь
trumpIndicator.setTrumpCard(card, animated: true)

// Скрыть/показать
trumpIndicator.hide(animated: true)
trumpIndicator.show(animated: true)
```

### 7. **Обновлённый PlayerNode.swift**
Добавлена поддержка карт и игровой статистики.

**Новые возможности:**
- Рука с картами (`hand: CardHandNode`)
- Отображение ставки и взяток
- Подсветка активного игрока
- Цветовая индикация выполнения ставки

**Использование:**
```swift
// Установить ставку
player.setBid(3, animated: true)

// Увеличить счётчик взяток
player.incrementTricks()

// Подсветить игрока (когда его ход)
player.highlight(true)

// Добавить карты в руку
player.hand.addCards([card1, card2, card3], animated: true)

// Сброс для новой раздачи
player.resetForNewRound()
```

## Интеграция в GameScene

В `GameScene.swift` добавлена полная интеграция:

```swift
// Создание игровых компонентов
private func setupGameComponents() {
    deck = Deck()
    
    trickNode = TrickNode()
    trickNode.centerPosition = CGPoint(x: width/2, y: height/2)
    addChild(trickNode)
    
    trumpIndicator = TrumpIndicator()
    trumpIndicator.position = CGPoint(x: width - 90, y: height/2)
    addChild(trumpIndicator)
}

// Раздача карт
private func dealCards() {
    deck.reset()
    deck.shuffle()
    
    let result = deck.dealCards(playerCount: playerCount, cardsPerPlayer: 3)
    
    for (index, player) in players.enumerated() {
        player.hand.addCards(result.hands[index], animated: true)
        player.hand.sortCardsStandard(animated: true)
    }
    
    trumpIndicator.setTrumpCard(result.trump, animated: true)
}
```

## Игровая логика

### Правила игры реализованы в:

1. **Card.beats()** - Сравнение карт с учётом козыря
2. **TrickNode.canPlayCard()** - Проверка валидности хода
3. **TrickNode.determineWinner()** - Определение победителя взятки
4. **Deck** - Правильная колода (36 карт, 2 джокера)

### Особенности реализации:

- ✅ Джокер бьёт любую карту
- ✅ Козырь бьёт некозырную карту
- ✅ Нужно ходить в масть
- ✅ Если нет масти - можно козырь
- ✅ Бубны и черви: 6-Туз (9 карт)
- ✅ Пики и крести: 7-Туз (8 карт)
- ✅ 6♦️ и 6♣️ заменены на джокеров

## Анимации

Все классы поддерживают анимации:

- Раздача карт с плавным появлением
- Переворот карт с 3D эффектом
- Плавное перемещение карт
- Пульсация при подсветке
- Веерная раскладка с анимацией
- Сбор взятки с перемещением к победителю

## Визуальный стиль

- **Карты**: Белый фон, чёткие символы, красные/чёрные масти
- **Джокер**: Жёлтоватый фон с эмодзи 🃏
- **Рубашка**: Синий фон с белым узором из ромбов
- **Подсветка**: Жёлтая пульсирующая рамка
- **Индикатор козыря**: Тёмный фон с золотой рамкой

## Примеры использования

### Раздача и сортировка
```swift
// Раздать карты
let deck = Deck()
deck.shuffle()
let result = deck.dealCards(playerCount: 4, cardsPerPlayer: 9)

// Раздать игрокам
for (index, player) in players.enumerated() {
    player.hand.addCards(result.hands[index], animated: true)
    
    // Отсортировать через секунду
    DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
        player.hand.sortCardsStandard(animated: true)
    }
}
```

### Ход игрока
```swift
// Получить возможные ходы
let playableCards = playerHand.filter { card in
    trickNode.canPlayCard(card, fromHand: playerHand, trump: currentTrump)
}

// Подсветить возможные карты
hand.highlightPlayableCards(playableCards)

// Сыграть карту
if let cardNode = hand.removeCard(selectedCard, animated: true) {
    let playedNode = trickNode.playCard(selectedCard, fromPlayer: playerNumber, animated: true)
}
```

### Завершение взятки
```swift
// Определить победителя
if let winner = trickNode.determineWinner(trump: currentTrump) {
    players[winner].incrementTricks()
    
    // Собрать карты
    let winnerPosition = players[winner].position
    trickNode.clearTrick(toPosition: winnerPosition, animated: true) {
        // Следующий ход
        nextTrick()
    }
}
```

## Производительность

- Все анимации оптимизированы для плавной работы
- Используются SKAction для аппаратного ускорения
- Минимальное количество нод на экране
- Эффективное управление памятью

## Расширяемость

Классы легко расширяются для:
- Добавления звуковых эффектов
- Различных тем оформления карт
- Дополнительных анимаций
- Сетевой игры
- AI противников

## Заключение

Реализован полный набор классов для профессионального отображения и управления игральными картами в SpriteKit, полностью соответствующий правилам игры Jocker.
